id: TASK-015
title: 'EXP: structured-output grammar-compile latency (cold vs warm schema)'
description: 'A/B: first call with a novel JSON schema (output_config.format) vs a

  second call reusing the SAME schema within 24h (cached grammar),

  comparing wall-clock/TTFT to isolate compile-time overhead. Separately,

  compare steady-state (warm-cache) decode tok/s with structured output

  active vs entirely absent, same prompt/context, to fill the F7 data gap.

  '
workstream: experiments
priority: 35
depends: []
scope: []
tags: [serial]
verification:
  - Result appended to docs/THROUGHPUT_RESEARCH.md, resolving this Open Question
acceptance: []
notes:
  - at: '2026-07-17T03:01:21'
    text: 'Dispatching a read-only/design fork to investigate output_config.format

  (structured outputs) wire shape and propose a cold-vs-warm schema test

  design. No code changes this wave (avoids contention with TASK-020''s edits

  to the same harness file) -- implementation deferred to a follow-up wave.

  '
status: completed
resolution: 'Structured-output cold-vs-warm: suggestive (3/3 pairs favor warm faster; cleanest pair shows warm doing MORE work in LESS time), not yet a clean effect size (n=3, uncontrolled length -- API rejects minItems/maxItems != 0/1, a new finding folded into F7). docs/THROUGHPUT_RESEARCH.md F18.'
created: '2026-07-17T01:16:35'
updated: '2026-07-17T03:41:15'
completed_at: '2026-07-17T03:41:15'
